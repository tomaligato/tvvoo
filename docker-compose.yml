services:
  tvvoo:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tvvoo
    restart: unless-stopped
    ports:
      - "7860:7860"
    environment:
      - PORT=7860
      # - VAVOO_DEBUG=1
    networks:
      - stremio_net

networks:
  stremio_net:
    external: true
